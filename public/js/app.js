(function(){
	/* Inicialización */
	var app = angular.module('store', []);
	/* Controladores */
	/*
	app.controller('ConceptosCtrl',function($scope) {
		$scope.conceptos = [{concepto: '', precio_unitario: 0, cantidad: 0}];
		$scope.agregarConcepto = function() {
			$scope.conceptos.push(jQuery.extend(true, {}, concepto_base));
		};
	});
	*/
	/* Directivas */
	
	/* Datos */
	/*
	var concepto_base =
		{
			"concepto": "",
			"precio_unitario" : 0,
			"cantidad" : 0
		};
	*/
})();
